import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Second Brain — AI-Powered Knowledge Management",
  description:
    "An AI agent that manages your personal knowledge, connects to Gmail and GitHub via Auth0 Token Vault, and takes actions on your behalf with secure approval flows.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="antialiased">{children}</body>
    </html>
  );
}
